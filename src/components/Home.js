import axios from 'axios'
import React, { useEffect } from 'react'

const Home = () => {

    useEffect(() => {
        axios.get() //localhost../
    })

    return (
        <div>
            <h2>You are not logged in</h2>
        </div>
    )
}

export default Home;
